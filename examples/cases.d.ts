// `examples/cases.js` is plain JS served straight to the browser demo page; this
// declaration lets the test suite import the same list without duplicating it.
export declare const cases: { title: string; code: string }[];
